language: c

compiler:
  - gcc-7
  - clang

env:
  matrix:
    - DEBUG=0
    - DEBUG=1

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-7

script:
  - make all
  - make test

branches:
  except:
    - /^\d+-/

